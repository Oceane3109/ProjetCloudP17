# Exemple d'environnement pour backend idp-api
# JWT secret (>=32 chars)
APP_JWT_SECRET=replace_this_with_a_strong_secret_of_at_least_32_chars

# Bootstrap manager account (optionnel)
APP_BOOTSTRAP_MANAGER_EMAIL=manager@local
APP_BOOTSTRAP_MANAGER_PASSWORD=manager

# Firebase (optionnel) - chemin vers JSON monté en secret/volume
# Exemple Docker: file:/run/secrets/firebase-service-account.json
APP_FIREBASE_SERVICE_ACCOUNT_PATH=
APP_FIREBASE_PROJECT_ID=
APP_FIREBASE_STORAGE_BUCKET=
